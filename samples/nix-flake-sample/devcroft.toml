[sandbox]
name = "nix-flake-sample"

[env]
provider = "nix"

# Uncomment to grant more than the project root (already allowed
# by default) or to change the network default:
#
# [filesystem]
# allow = ["."]
# read = ["/path/to/a/read-only/dir"]
#
# [network]
# default = "deny"
# allow = ["api.example.com"]
